{% if event.category.imageUrl %}
    <figure>
        <img src="{{ event.category.imageUrl }}" alt="{{ event.title }} - Catégorie : {{ event.category.name }}">
        <figcaption>{{ event.title }} - Catégorie : {{ event.category.name }}</figcaption>
    </figure>
{% endif %}